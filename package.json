{
  "name": "@thasmo/stylelint-config",
  "version": "0.1.0",
  "description": "Stylelint configuration for SCSS-based stylesheets.",
  "license": "MIT",
  "main": "dist/configuration.json",
  "scripts": {
    "build": "node build.js",
    "test": "ava",
    "postinstall": "node build.js"
  },
  "devDependencies": {
    "ava": "1.0.0-beta.6",
    "fast-glob": "^2.2.2",
    "stylelint": "^9.0.0"
  },
  "dependencies": {
    "stylelint-scss": "^3.2.0"
  },
  "peerDependencies": {
    "stylelint": "^9.0.0"
  }
}
